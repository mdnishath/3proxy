# === Secrets — NEVER commit these ===
# SSH private key that grants root to the VPS
keys/panel_id_ed25519
keys/panel_id_ed25519.pub

# Random shared secret generated by fresh-setup.ps1 (unique per install)
keys/agent-secret.txt

# Router rotation creds (hashed admin user/pass for each pocket router)
# The .example template IS committed; the real file with hashes is not.
config/routers.json

# === Runtime artifacts (regenerated on every start) ===
config/3proxy.cfg
config/adapters.json
config/passwd
logs/

# === Junk from earlier buggy redirections in batch files ===
51081
51082
System.Collections.Hashtable
_NUL

# === Claude Code / editor local state ===
.claude/
.vscode/
.idea/

# === OS / tooling ===
Thumbs.db
.DS_Store
